#include <iostream>
#include <vector>

using namespace std;

int main()
{
	// Defining number of rows and columns in matrix
	int num_row = 3;
	int num_col = 3;

	// Vector of vectors declaration
	vector<vector<int>> arr(num_row, vector<int>(num_col));

	return 0;
}
